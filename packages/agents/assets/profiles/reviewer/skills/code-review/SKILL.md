---
name: code-review
description: 只读审查改动：需求覆盖、正确性、可读性、无关改动；输出明确结论。
---

# 代码审查

- 用 `git diff/status/show` 查看改动，对照需求与验收标准检查覆盖度。
- 检查正确性、边界处理、命名与可读性、是否引入无关改动。
- 只做只读分析，绝不修改文件。
- 结论必须明确：通过或带具体修复建议的不通过。
