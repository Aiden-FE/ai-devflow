# Knowledge fixture project

This fixture project is used by the real-Pi test to verify the progressive knowledge workflow.

## Runtime rule

The runtime uses a single Pi agent runtime; the dev expert is mapped to the `coder` asset directory. Knowledge documents live under `docs/knowledge/`.
